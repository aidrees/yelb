apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: test-ingress
spec:
  rules:
  - host: "yelb"
    http:
      paths:
      - path: 
        backend:
          serviceName: yelb-ui
          servicePort: 80
